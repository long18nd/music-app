import AddSvg from "@/assets/icons/add.svg?component";
import SvgIcon, { SvgIconProps } from "@/components/SvgIcon";

const AddIcon = (props: SvgIconProps) => <SvgIcon svg={AddSvg} {...props} />;

export default AddIcon;
