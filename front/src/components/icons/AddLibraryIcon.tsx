import AddLibrarySvg from "@/assets/icons/add_library.svg?component";
import SvgIcon, { SvgIconProps } from "@/components/SvgIcon";

const AddLibraryIcon = (props: SvgIconProps) => <SvgIcon svg={AddLibrarySvg} {...props} />;

export default AddLibraryIcon;
